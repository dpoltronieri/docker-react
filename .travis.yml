language: node_js
node_js:
  - 0.12
sudo: required
services:
  - docker

before_install:
  - docker build -t poltronieri/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true poltronieri/docker-react npm run test